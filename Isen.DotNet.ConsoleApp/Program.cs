using System;
using Isen.DotNet.Library;
using Isen.DotNet.Library.Repositories.InMemory;
using Isen.DotNet.Library.Repositories.Interfaces;

namespace Isen.DotNet.ConsoleApp
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            ICategorieRepository categorieRepository = new InMemoryCategorieRepository();
            ICommuneRepository communeRepository = new InMemoryCommuneRepository();
            IPointInteretRepository pointInteretRepository = new InMemoryPointInteretRepository(communeRepository, categorieRepository);
            */
        }
    }
}
